# Trading Signals Dashboard

Проект: торговый терминал с автоматическим анализом рынка, расчетом индикаторов и веб-дашбордом.

---

## 🚀 Описание проекта

Веб-приложение для трейдеров, которое:

- принимает сигналы из TradingView (webhook)
- получает рыночные данные (Crypto + MOEX)
- рассчитывает технические индикаторы
- анализирует рынок и формирует сигналы
- сохраняет данные в БД
- отображает всё в веб-дашборде

---

## ⚙️ Основной функционал

### 1. Прием сигналов

- HTTP webhook от TradingView
- автоматическая обработка

### 2. Получение данных

- Crypto: Bybit API
- MOEX: ISS API Московской биржи

### 3. Аналитика

Рассчитываются индикаторы:

- SMA 200
- EMA
- RSI
- MACD
- Chaikin Money Flow (CMF)

---

### 4. Генерация сигналов

Система анализирует:

- тренд (SMA)
- импульс (MACD)
- перекупленность/перепроданность (RSI)
- поток денег (CMF)

И выдает:

- `BUY`
- `SELL`
- `HOLD`

---

### 5. Хранение данных

- PostgreSQL
- сохраняются:
    - цена
    - индикаторы
    - сигнал
    - время

---

### 6. Веб-дашборд

- LIVE блок (текущая цена + индикаторы)
- таблица сигналов с цветовой индикацией (BUY / SELL / HOLD)
- фильтр по тикеру
- переключение таймфрейма (15m / 1h / 1d)
- поддержка:
    - Crypto (BTCUSDT и др.)
    - MOEX (SBER, GAZP, LKOH, VTBR)

---

## Архитектура

TradingView → Django API → Анализ → PostgreSQL→Dashboard (HTML + JS)

---

## Технологии

### Backend

- Django
- Django REST Framework

### Data

- NumPy
- Pandas (частично)

### База данных

- PostgreSQL

### API

- Bybit API
- MOEX ISS API

### DevOps

- Docker
- Docker Compose

---

## Запуск проекта (Docker)

### 1. Клонирование

```bash
git clone <repo>
cd project
```

### 2. Запуск

```
docker-compose up --build
```

### 3. Применение миграций

```docker exec -it django_app python manage.py migrate```

### 4. Создание суперпользователя

```docker exec -it django_app python manage.py createsuperuser```

### 5. Открыть проект

Dashboard:

```
http://localhost:8000/dashboard/
```

### API:

- Webhook (TradingView)

Endpoint:

```
POST /webhook/
```

**Пример тела запроса:**

```
{
  "symbol": "BTCUSDT",
  "timeframe": "15m",
  "signal_type": "BUY"
}
```

**Описание полей:**

- symbol — торговая пара (BTCUSDT, ETHUSDT, SBER и т.д.)
- timeframe — таймфрейм (15m / 1h / 1d)
- signal_type — тип сигнала от TradingView (необязательный)

---

### Анализ рынка

```
GET /analyze/?symbol=BTCUSDT&timeframe=1h
```

**Параметры:**

- symbol — тикер
- timeframe — таймфрейм (по умолчанию 1h)

### Список сигналов

Фильтры:

```
/signals/?symbol=BTCUSDT
/signals/?timeframe=15m
/signals/?signal_type=BUY
```

---

### 6. Автообновление данных

- каждые 5 секунд обновляется:

``` 
    цена
    индикаторы
    сигнал
    Поддержка MOEX
```

### 7. Поддерживаются тикеры:

~~~
SBER
GAZP
LKOH
VTBR
~~~

---

#### Если рынок закрыт:

Используется последняя цена (cache)
данные не пропадают

---

### 📦 Структура проекта

signals/
├── services.py # индикаторы и стратегия
├── views.py # API endpoints
├── market.py # crypto данные
├── moex_data.py # MOEX API
├── models.py # БД
├── serializers.py
├── templates/signals
│ └── dashboard.html

---

### Тестирование

Проект использует pytest для тестирования.

Запуск тестов:

```
docker exec -it django_app pytest -v
```

---

### Будущие доработки

- 📩 Telegram уведомления
- 💰 платные подписки
- 📉 уровни Фибоначчи
- 📊 графики (TradingView / Chart.js)

---

## Вывод

### Система позволяет:

- автоматически анализировать рынок
- получать сигналы в реальном времени
- отслеживать историю
- работать с крипто и фондовым рынком

---

### Обновление проекта:

- Добавлен мульти-таймфрейм анализ
- Реализована поддержка нескольких таймфреймов:
    1. 15m — краткосрочная торговля (повышенное количество сигналов)
    2. 1h — среднесрочный анализ
    3. 1d — долгосрочный тренд

#### Функционал:

Пользователь может переключать таймфрейм в интерфейсе
данные автоматически обновляются без перезагрузки страницы
сигналы пересчитываются в зависимости от выбранного интервала

### Система теперь поддерживает:

- мульти-таймфрейм анализ (15m / 1h / 1d)
- live-дашборд в реальном времени
- автоматическую генерацию торговых сигналов
- работу с Crypto и MOEX
- подготовлена архитектура для интеграции Celery
- технические индикаторы (RSI, MACD, CMF, SMA)

---

## Автор

Филиппов Алексей Сергеевич
